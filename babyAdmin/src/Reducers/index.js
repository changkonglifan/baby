import {
  createReducer
} from '../util';
// import types from '../store/types';
const initialState = {
  name: '菜菜',
  introduce:'走在全栈路上的前端开发工程师'
};
export default createReducer(initialState, {

});
